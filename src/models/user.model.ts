export interface UserModel {
    login: string;
    password: string;
    index: string;
}

export interface Message {
    type: string;
    data: string;
    id: number;
}

export interface UsersModel {
    name: string;
    index: string;
}